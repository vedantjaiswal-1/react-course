import React from "react";
import { Button, Container } from "react-bootstrap";

export const About = (props: any) => {
  return (
    <div>
      <Container fluid>
        <Button>Click</Button>
      </Container>
    </div>
  );
};
